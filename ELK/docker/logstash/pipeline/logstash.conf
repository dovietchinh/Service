input {
  beats {
    port => 5044
  }
  tcp {
    port => 5000
  }
}
output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "elastic1"
    password => "changeme"
    index => "log-demo"
  }
  stdout { codec => rubydebug }
}
